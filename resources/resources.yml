resources:
  jobs:
    unit_tests:
      name: "[${bundle.target}] Unit Tests"

      tasks:
        - task_key: run_unit_tests
          notebook_task:
            notebook_path: ../tests/test_finance_pipeline.py
          libraries:
            - pypi:
                package: pytest

